<form method="post" action="{{ path('user_demande_paiement_delete', {'id': demande_paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_paiement.id) }}">
    <button class="btn">Delete</button>
</form>
